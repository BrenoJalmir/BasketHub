<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BasketHub">
        <activity
            android:name=".AppHub"
            android:exported="true"
            android:theme="@style/Theme.BasketHub">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScoreboardApp"
            android:exported="true"
            android:label="@string/scoreboard_app"
            android:theme="@style/Theme.BasketHub">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CalculatorApp"
            android:exported="true"
            android:label="@string/calculator_app"
            android:theme="@style/Theme.BasketHub">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GamesHistoryApp"
            android:exported="true"
            android:label="@string/matches_history"
            android:theme="@style/Theme.BasketHub">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddMatchActivity"
            android:exported="false"
            android:label="@string/add_match"
            android:theme="@style/Theme.BasketHub" />
    </application>

</manifest>